# Data providers

The first live provider is football-data.org. It provides competition, team and match data through a documented API and keeps credentials server-side.

Player-level advanced metrics such as xG/xA should be added only from a source whose current access terms permit our intended use. The model therefore separates fixture/team data from player-stat providers.
